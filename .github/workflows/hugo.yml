name: Deploy Hugo site to Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
<<<<<<< HEAD
=======

>>>>>>> 2b964cf (Lưu lại bài viết trước khi đồng bộ)
permissions:
  contents: read
  pages: write
  id-token: write
<<<<<<< HEAD
=======

>>>>>>> 2b964cf (Lưu lại bài viết trước khi đồng bộ)
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
<<<<<<< HEAD
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.146.0' # ÉP BUỘC BẢN MỚI
          extended: true
      - name: Build with Hugo
        run: hugo --minify
=======

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          # ÉP BUỘC dùng bản mới nhất để khớp với yêu cầu của PaperMod
          hugo-version: '0.146.0' 
          extended: true

      - name: Build with Hugo
        # Thêm flag --gc và --minify để tối ưu hóa
        run: hugo --gc --minify

>>>>>>> 2b964cf (Lưu lại bài viết trước khi đồng bộ)
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public
<<<<<<< HEAD
=======

>>>>>>> 2b964cf (Lưu lại bài viết trước khi đồng bộ)
  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4